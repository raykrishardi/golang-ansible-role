[Unit]
Description=Hello World Go App
After=network.target

[Service]
User=root
Group=root
ExecStart={{go_example_hello_world_binary_full_path}}
Restart=on-failure
RestartSec=5

# Set environment variables if required
# Environment=ENV_VAR=value

[Install]
WantedBy=multi-user.target
